spring:
  redis:
    host: localhost
    port: 6379
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/moonrise
    username: root
    password: ssafyssafy
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: none
  properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 1000
logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace
server:
  port: 9001